services:
  valkey:
    image: valkey/valkey
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/data
    command: ["valkey-server", "--requirepass", "mypassword"]

volumes:
  valkey_data: